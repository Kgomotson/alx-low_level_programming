#include <stdio.h>
/**
 * main - Entry point
 * @str - input value
 * Return: Always 0 (Success)
 */
int main(void)
{
char *str = "\"Programming is like building a multilingual puzzle";
puts(str);
return (0);
}
